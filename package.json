{
  "name": "chrome-fip",
  "version": "0.7.7",
  "description": "FIP Radio Chrome Extension for an in-browser listening and scrobbling experience.",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": ">=4.0.0"
  },
  "scripts": {
    "blah": "env",
    "test": "NODE_ENV=test ./node_modules/karma/bin/karma start",
    "posttest": "eslint --ignore-pattern '**/bundle.js' ./src",
    "pretest": "npm run build",
    "build": "npm-run-all --parallel 'build:*'",
    "build:now-playing": "browserify -e ./src/now-playing/app.js -o ./src/now-playing/bundle.js",
    "build:background": "browserify -e ./src/background/app.js -o ./src/background/bundle.js",
    "pre-build": "npm test",
    "start": "static -H ${npm_package_http_headers} --gzip -p ${PORT:-3000} .",
    "watch": "BUILD_CHANNEL=dev npm-run-all --parallel 'watch:*' start",
    "watch:background": "watchify -v -e ./src/background/app.js -o ./src/background/bundle.js",
    "watch:now-playing": "watchify -v -e ./src/now-playing/app.js -o ./src/now-playing/bundle.js"
  },
  "http": {
    "headers": "{\"Access-Control-Allow-Origin\":\"*\",\"Access-Control-Allow-Methods\":\"GET\",\"Access-Control-Allow-Headers\":\"Content-Type\"}"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/oncletom/chrome-fip.git"
  },
  "keywords": [
    "fip",
    "radio",
    "chrome",
    "extension",
    "audio",
    "live",
    "streaming",
    "france"
  ],
  "author": "Thomas Parisot (https://oncletom.io)",
  "license": "MIT",
  "readmeFilename": "README.md",
  "browserify": {
    "transform": [
      [
        "sassify",
        {
          "auto-inject": true
        }
      ],
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ],
      "envify"
    ]
  },
  "devDependencies": {
    "babel-eslint": "^4.1.6",
    "babel-preset-es2015": "^6.2.0",
    "brfs": "^1.4.1",
    "browserify": "^12.0.1",
    "eslint": "^1.10.1",
    "karma": "^0.13.0",
    "karma-browserify": "^4.4.0",
    "karma-chrome-launcher": "^0.2.0",
    "karma-firefox-launcher": "^0.1.7",
    "karma-mocha": "^0.2.0",
    "karma-phantomjs-launcher": "^0.2.0",
    "karma-sinon-chai": "^1.0.0",
    "mocha": "^2.0.0",
    "node-static": "^0.7.7",
    "npm-run-all": "^1.3.2"
  },
  "dependencies": {
    "angular": "^1.4.0",
    "babelify": "^7.2.0",
    "blueimp-md5": "^1.1.1",
    "envify": "^3.4.0",
    "lodash": "^3.10.1",
    "machina": "^1.1.2",
    "sassify": "^0.9.1"
  }
}
